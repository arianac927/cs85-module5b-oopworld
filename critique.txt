* ChatGPT Prompt:create a method that can add an item to this array of books with multiple property values in php, $books = [ new Book(1, "Shadow and Bone", "Leah Bardugo", 358, "Fantasy"), new Book(2, "The Deal", "Elle Kennedy", 342, "Romance"), new Book(3, "The Midnight Library", "Matt Haig", 288, "Fantasy"), new Book(4, "The List of Suspicious Things", "Jennie Godfrey", 416, "Mystery"), new Book(5, "The Summer I Turned Pretty", "Jenny Han", 288, "Romance") ];* ChatGPT Answer:$books = [new Book(1, "Shadow and Bone", "Leigh Bardugo", 358, "Fantasy"),new Book(2, "The Deal", "Elle Kennedy", 342, "Romance"),new Book(3, "The Midnight Library", "Matt Haig", 288, "Fantasy"),new Book(4, "The List of Suspicious Things", "Jennie Godfrey", 416, "Mystery"),new Book(5, "The Summer I Turned Pretty", "Jenny Han", 288, "Romance")];// Add a new bookaddBook($books, new Book(6,"Fourth Wing","Rebecca Yarros",512,"Fantasy"));// Display the booksforeach ($books as $book) {echo $book->getTitle() . "<br>";}* I then asked it the following to adjust the code:can you redo this to prompt the user to add the book entry to existing array in php*It's next answer was:<?php$books = [    new Book(1, "Shadow and Bone", "Leigh Bardugo", 358, "Fantasy"),    new Book(2, "The Deal", "Elle Kennedy", 342, "Romance"),    new Book(3, "The Midnight Library", "Matt Haig", 288, "Fantasy"),    new Book(4, "The List of Suspicious Things", "Jennie Godfrey", 416, "Mystery"),    new Book(5, "The Summer I Turned Pretty", "Jenny Han", 288, "Romance")];function addBook(array &$books): void{    echo "Enter Book ID: ";    $id = (int) trim(fgets(STDIN));    echo "Enter Title: ";    $title = trim(fgets(STDIN));    echo "Enter Author: ";    $author = trim(fgets(STDIN));    echo "Enter Number of Pages: ";    $pages = (int) trim(fgets(STDIN));    echo "Enter Genre: ";    $genre = trim(fgets(STDIN));    $books[] = new Book($id, $title, $author, $pages, $genre);    echo "\nBook added successfully!\n";}// Call the methodaddBook($books);// Display all booksforeach ($books as $book) {    echo $book->getId() . " | "        . $book->getTitle() . " | "        . $book->getAuthor() . " | "        . $book->getPages() . " pages | "        . $book->getGenre() . PHP_EOL;}* CRITIQUE:After seeing the answer that was generated, I was hopeful it would work right away; however, I was still experiencing several syntax errors and other errors. I realized after inputting the recommended code, there wasn't any mention of the input form, so I had to add that into the code. I also had to fix the fgets(STDIN) it generated for me since I kept getting an error for this. As aforementioned, I had to re-ask the question to achieve what I initially wanted to see. I also added an if statement within the addBook function in case a user inputted the wrong characters or empty entries for the book tracker. I can see how efficient this would be, but it doesn't always generate a correct answer, like in my case. You would have to be super specific with what you ask, which probably would be a given for some, but people who aren't great with descriptive prompts may experience issues with what they generate.